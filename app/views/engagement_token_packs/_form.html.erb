<%= simple_form_for(@engagement_token_pack) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <% if @engagement_token_pack.new_record? %>
      <%= f.input :name %>
      <%= f.input :number_of_tokens %>
    <% end %>
    <%= f.input :member_price, label: 'Price', :input_html => {value: number_with_precision(f.object.member_price, precision: 2) } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
